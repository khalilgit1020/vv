import 'package:flutter/material.dart';
import 'package:vehicle_tracking1/constanc.dart';

class AboutScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: primaryColor,
      ),
      body: Container(
        child: Column(
          children: [
            Text("data"),
          ],
        ),
      ),
    );
  }
}
